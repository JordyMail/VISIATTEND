# VISIATTEND

Installation:
pnpm install

Quick start:
pnpm run db:test
pnpm dev

Notes:
- UI components from shadcn used by the app are already committed in this repository.
- Runtime packages such as axios, js-cookie, crypto-js, framer-motion, mssql, bcrypt, and jsonwebtoken are installed through `pnpm install`.
